<!-- Append-only decision register for Pairwise Behavioral Signatures research.
     Never edit or remove existing rows. To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | setup | architecture | LLM routing layer | LiteLLM | Single interface for 4 providers. Built-in retry, cost tracking, JSON mode, budget caps. Avoids 4 separate SDK integrations. | No — foundational |
| D002 | setup | architecture | Simulation engine | Concordia v2.0 | Entity-Component architecture with native per-agent LLM override, simultaneous engine, checkpointing, JSON logging. Evaluate built-in marketplace before building custom. | No — foundational |
| D003 | setup | architecture | Analysis stack | Polars + statsmodels + scikit-learn + NetworkX + sentence-transformers + seaborn | Polars for speed on 50K+ rows; statsmodels for mixed-effects and non-parametric tests; sklearn for classifier; NetworkX for trade graphs; seaborn for heatmaps. | Yes — if bottlenecks found |
| D004 | setup | scope | Model families | Llama 3.3 70B, DeepSeek V3.2, Gemini 2.5 Flash, Mistral Small 3.1 | 4 architecturally distinct families (dense transformer, MoE, hybrid-reasoning Flash, small efficient). All paid APIs with verified pricing. Newer versions deliberately excluded (study is about families). | No — paper scoping decision |
| D005 | setup | cost | Budget cap | $80 hard cap via LiteLLM router | Protects ₹12,500 total budget. Real spend ~$32; 3× buffer covers reruns and debugging. | No — financial constraint |
| D006 | setup | simulation | Game design | Trade Island: 6 agents, 25 rounds, 3VP equal VP values, 1G/round hunger | Equal VP prevents degenerate strategies; grain hunger creates scarcity-driven interdependency. Both confirmed in blueprint §3. | Yes — if Phase 0 reveals degenerate behavior |
| D007 | setup | research | Reflection mode | Agents reflect with own model family; DeepSeek agents may use reasoner mode for reflection | Preserves behavioral signatures. Reasoner mode for reflection adds depth at same per-token cost (identical pricing for V3.2). | Yes — if reasoner tokens inflate beyond budget |
| D008 | setup | research | GM model | Mistral Small 3.1 as Game Master | Cheapest capable model ($0.10/$0.30). GM confound mitigated by 10-game sensitivity test with Llama GM in Phase 0. | Yes — switch to Llama GM if sensitivity test shows confound |
| D009 | setup | infrastructure | Python environment | venv inside research/model-family/.venv/ | Isolated from system Python 3.14. Prevents dependency conflicts. Version-pinned requirements.txt for reproducibility. | No — reproducibility requirement |
| D010 | setup | version control | Git strategy | New dedicated git repo in research/model-family/ | Clean history for research artifact. Easy to push to GitHub for OSF + open-source deliverables. Separate from parent baagad-ai repo. | No — clean artifact requirement |
| D011 | setup | research-integrity | Pre-registration | OSF pre-registration before Phase 1 | Analysis scripts committed as stubs before any data collected. Locks H1-H4 hypotheses. Account creation is M001/S05 task. | No — scientific integrity |
| D012 | setup | prompt | Prompt format | Compact prefix-first templates (blueprint §2.2-2.5) with Phase 0 ablation to validate | 60-70% cache hit rate reduces cost from ~$80 to ~$32. Format validated in Phase 0 before any Phase 1 games run. | Yes — switch to verbose if <90% JSON parse rate on any model |
| D013 | setup | simulation | Pairwise configuration | 3v3 split with complementary resource specialties | Balanced agent counts. Complementary specialties force cross-family trading. Specialty assignments randomized per game and recorded as covariate. | Yes — if Phase 0 reveals imbalanced outcomes |
| D014 | setup | daily-cadence | Run cadence | Decide after Phase 0 (manual vs automated) | Phase 0 calibration games will reveal reliability. If game stability >95%, automate. If debugging needed frequently, stay manual. | Yes — reassess after Phase 0 |
| D015 | setup | paper | Paper format | Short paper for NeurIPS 2026 workshop first, then expand to AAMAS 2027 full paper | Workshop short paper deadline is September 2026 — achievable. Full paper leverages same data, expands related work and analysis. | No — venue strategy |
| D016 | setup | gemini | Gemini thinking mode | Disable thinking mode for all agent action calls (thinking_budget=0) | Thinking mode causes 5-12× output token inflation from hidden reasoning tokens. At $2.50/1M output, this would 5-12× Gemini cost. Enable only for reflection if desired. | No — cost control |
| D017 | setup | deepseek | DeepSeek JSON mode | Basic json_object mode only (no json_schema enforcement). Include schema in prompt text. | DeepSeek does NOT support json_schema enforcement per blueprint §3. Schema in prompt is the workaround. | No — provider constraint |
| D018 | setup | mistral | Mistral version pinning | Pin to mistral-small-2506 (dated version ID), NOT mistral-small-latest | Blueprint specified 3.1-2503 but that ID is not available on this account. 2506 is the current dated version (mistral-small-latest resolves to it as of March 2026). Pinned for reproducibility. | No — reproducibility requirement |
| D019 | M001/S01 | infrastructure | DeepSeek API access | OpenRouter proxy (openrouter/deepseek/deepseek-chat) instead of direct DeepSeek API | Direct DeepSeek API does not accept payments from India. OpenRouter is an aggregator that accepts international cards and carries DeepSeek V3 at near-identical pricing (~$0.26/$0.38 vs $0.28/$0.42 direct). Same model, same behavior. | No — payment constraint |
| D020 | M001/S01 | gemini | Gemini thinking disable method | thinking={'type':'disabled','budget_tokens':0} passed to litellm.completion | This maps to thinkingConfig.thinkingBudget=0 in the Google API request. Verified: reasoning_tokens=None when correctly applied. thinking={'type':'disabled'} alone is insufficient (still burns ~25 reasoning tokens). thinkingConfig as extra_body rejected by API (unknown field). | No — cost control, verified working |
| D021 | M001/S01 | gemini | Gemini JSON mode | Do NOT use response_format json_object for Gemini | json_object mode causes empty responses (content=None, finish_reason=length) when thinking tokens consume the token budget. Use plain completion + markdown fence stripper in parser. Paid tier required — free tier hits 429 immediately on burst calls (20 req/day limit on gemini-2.5-flash free tier). | No — verified working approach |
| D022 | M001/S01 | gemini | Gemini max_tokens | 200 minimum for agent calls (not 150 as in blueprint) | At max_tokens=150 with thinking partially active, output tokens were consumed by thinking chains leaving 0 for actual content. 200 provides safe headroom even if thinking budget leaks slightly. | Yes — tune if thinking fully suppressed in future litellm versions |
| D023 | M001/discuss | architecture | Concordia integration depth | Custom game loop — do NOT use Concordia entity/component/simultaneous-engine system | Concordia's LanguageModel interface is sample_text(prompt: str) — text completion, not chat completions. Per-provider workarounds (D018-D022) require litellm chat kwargs (thinking={}, response_format={}) that cannot pass cleanly through that interface. Custom ~200-line loop calls litellm.completion() directly with full control. Concordia's marketplace prefab is a clearing-house (bids/asks), not bilateral proposals — unusable for Trade Island. Concordia Inventory component may be referenced but not required. Installed version is gdm-concordia==2.4.0 (not 2.0.0 as blueprint assumed). | No — custom loop is foundational to S02+ |
| D024 | M001/discuss | deepseek | DeepSeek reflection model | openrouter/deepseek/deepseek-r1 for reflection calls; V3.2 (openrouter/deepseek/deepseek-chat) for all action/response/GM calls | Blueprint D007 stated 'no extra per-token cost' for R1 — incorrect. OpenRouter prices R1 at $0.55/$2.19 vs V3.2 $0.26/$0.38 (output ~5.8× more expensive). User accepted ~$0.45 extra across Phase 0. Cap reflection max_tokens=800 to prevent runaway. | Yes — revert to V3 if Phase 0 costs spike |
| D025 | M001/discuss | infrastructure | Checkpoint/resume strategy | JSON per-round files: checkpoint_r{N:02d}.json with full game state | Simple, deterministic, no extra dependencies. Single-threaded sequential games don't need SQLite durability. Resume loads highest-numbered checkpoint, continues from round N+1. State includes: round number, inventories, VPs, reflections, random seed. | No |
| D026 | M001/planning | simulation | S02 scope — Concordia marketplace re-evaluation | Eliminated from S02 plan | D023 is already settled (custom loop). The S02 sketch included a T02 "decision gate" re-evaluating the marketplace. This is not needed — D023 is recorded and closed. Removing the evaluation task avoids wasted effort and ambiguity about whether the decision is open. | No — D023 supersedes |
| D027 | M001/planning | prompts | S03 depends on S02 schema before template authoring | S03 starts after S02 JSONL schema is finalized | Prompt template variables (round, agent_id, inventory fields) must match the finalized JSONL schema. Authoring templates before schema is stable risks mismatched field names that break either the logger or the parser. The stable schema is the S02 → S03 boundary contract. | No |
| D028 | M001/planning | research | DeepSeek R1 cost risk in Phase 0 is low; Phase 1 is the real exposure | No R1 mitigation needed for Phase 0 | Phase 0 has only 5 DeepSeek calibration games × 5 reflections = 25 R1 calls. At $2.19/1M output with 800-token cap, worst case ~$0.044. Well within Phase 0 $1.50 budget. The earlier estimate of ~$8 assumed most of 30 games were DeepSeek-mono — incorrect for Phase 0 design. Revisit R1 cost monitoring at M002 planning. | Yes — revisit for Phase 1 |
| D029 | M001/S02/planning | simulation | round_end JSONL schema: one flat line per agent | Emit one `round_end` JSON line per agent with flat fields `game_id, model_family, round, agent_id, vp` (not a nested `vp_state` dict) | H1 analysis stub expects a flat Polars DataFrame filtered on `pl.col("round") == 25` with per-row `vp` values. A nested dict requires an explode step not present in the pre-registered stub. Flat lines are also simpler to grep and audit. | No — locked by pre-registered H1 analysis stub |
| D030 | M001/S02/planning | simulation | Checkpoint/JSONL write ordering enforced at plan level | JSONL flush+fsync before checkpoint write — order explicit in S02 T04 must-haves | If checkpoint is written before JSONL flush and process crashes, resume re-runs the round from checkpoint state, producing duplicate JSONL events. Writing JSONL first ensures crash recovery can detect the duplicate-free resume point. Duplicates would corrupt H1/H2 analysis counts. | No — data integrity requirement |
| D031 | M001/S02/planning | simulation | GM parse failure fallback: all-invalid, game continues | On GM JSON parse failure after 2 retries: log `gm_parse_failure` event, mark all proposals in the batch as invalid, continue game | Alternative (crash game) would corrupt a 25-round run on a transient Mistral hiccup. All-invalid is conservative (no resource transfer) and keeps the game state consistent. The failure event gives a future agent the raw GM output for diagnosis. | Yes — could add a third retry with simplified prompt if failure rate >5% in Phase 0 |
| D032 | M001/S02/planning | simulation | sentence-transformers installation deferred | Skip `sentence-transformers` in T01; add comment in requirements-lock.txt | `torch` wheel compatibility with Python 3.14 is uncertain as of March 2026. sentence-transformers is only needed for M004 analysis (behavioral fingerprinting). Deferring avoids a ~1.5GB install that may fail or require source compilation. Flag for S03/S04 boundary check. | Yes — verify at S04 completion |
| D033 | M001/S02/T01 | simulation | GameLogger file buffering | buffering=1 (line-buffered) on the JSONL file handle | Default block-buffering causes data written by log() to be unreadable until flush() is called explicitly. Line buffering ensures each log() call is immediately on disk — required for crash-resume (last round readable without explicit flush) and real-time jq inspection. flush()+fsync() remains for guaranteed checkpoint durability. | No — correctness requirement |
| D034 | M001/S02/T03 | simulation | Resolution dataclass (not dict) returned by resolve_trades() | Resolution dataclass with typed fields: trade_idx, valid, reason, proposer, responder, proposer_model, responder_model, pairing, give_resource, want_resource, accepted, give_qty, want_qty | Typed dataclass avoids dict key typos in T04 game.py when accessing trade results. give_qty and want_qty added so game.py can mutate canonical inventories from the resolution without re-parsing the original proposal. | No — T04 depends on this interface |
| D035 | M001/S02/T03 | simulation | GM LLM parse failure fallback refined: inventory checks remain the binding gate | On GM LLM JSON parse failure after 2 retries: log gm_parse_failure, default LLM verdicts to approved, let inventory validation still run (not all-invalid) | D031 (all-invalid fallback) was a planning decision. Implementation revealed that all-invalid on transient LLM failure would corrupt valid trades for agents with sufficient inventory on a Mistral hiccup. Refined: LLM verdict is a fairness check layer; inventory check is the binding gate. Net effect: trades with sufficient inventory are not wrongly blocked by transient LLM errors; gm_parse_failure event still provides full diagnostic visibility. | Yes — could revert to all-invalid if Phase 0 shows LLM fairness check catching real violations |
| D036 | M001/S02/T04 | simulation | Cost tracking via module-level function patching in GameRunner | GameRunner temporarily replaces `call_llm` in agent and gm module __dict__ during game run to accumulate per-call costs into a list | Agent and GM modules import call_llm with `from ... import call_llm` which binds to the module's global dict. Patching `agent_mod.call_llm` at the module attribute level makes functions in that module use the patched version (Python global lookup resolves through module __dict__ at call time, not at import time). Cost bucket is summed into `total_cost_usd` at game_end. Restore happens in finally block. | Yes — could use litellm success_callback instead if patching becomes fragile |
| D037 | M001/S02/T04 | research | Mistral-mono game: 0 accepted trades in first real run (behavioral finding) | All 6 Mistral-small-2506 agents consistently declined trade proposals in the 25-round acceptance run | 115 trade proposals were generated (agents chose action_type="trade"); all responders declined. Primary reasons: responder_declined (78), responder_insufficient_wood (33). This is emergent LLM behavior, not a code bug — agents prefer building (all 6 chose build on most rounds). The trade execution path is exercised (proposals + GM resolution + inventory check); acceptance requires LLM consent. The `≥1 accepted trade` slice verification criterion is unmet in this behavioral run. Phase 0 calibration (S04) should investigate prompt adjustments to increase trade acceptance rates. | Yes — monitor in Phase 0 calibration games |
| D038 | M001/S03/planning | prompts | Prompt module function signatures take explicit args, not agent object | `build_act_messages(agent_id, model_family, round_num, inventory, vp, buildings_built, all_agents_vp, memory, buildings_config)` — no Agent object or raw game_state dict in signature | Pure function layer: prompt modules cannot import from simulation (would create circular dependency). Explicit args also make token budget testing straightforward (pass synthetic dicts; no full game state needed). Callers extract what the prompt needs from agent state and game_state before calling. | No — circular import would break the module boundary |
| D039 | M001/S03/planning | prompts | phase0 config: 2 Llama + 2 DeepSeek + 1 Gemini + 1 Mistral (a0-a1/a2-a3/a4/a5) | _mixed_4family() builder invoked by from_name('phase0') | All 4 families present in every calibration game satisfies the S04 "all 4 providers route correctly in a single game" success criterion. 2:2:1:1 balance gives Llama and DeepSeek equal representation (the two most behaviorally distinct families per D004). Fixed slot assignment (not randomized) ensures reproducible routing; specialty randomization in S04 provides the scientific variance. | Yes — could rebalance if Phase 0 shows one family dominating due to slot advantage |
| D040 | M001/S03/T03 | prompts | Lazy import of strip_md and PROVIDER_KWARGS in json_utils.py to break circular import | `from src.simulation.llm_router import strip_md` moved inside `parse_agent_response()`; `from src.simulation.llm_router import PROVIDER_KWARGS` moved inside `get_completion_kwargs()` | `src.prompts.json_utils` imports `src.simulation.llm_router`, which triggers `src.simulation.__init__`, which imports `GameRunner` → `agent.py` → `src.prompts.agent_action` → `building_decision` → back to `json_utils` before it is fully initialised. Lazy import defers the dependency to first function call; by then both packages are fully loaded. Does not affect correctness — PROVIDER_KWARGS is a module-level dict that never changes at runtime. | Yes — could also fix by removing eager GameRunner import from src.simulation.__init__, but that's a wider surface change |
| D041 | M001/S04/planning | prompts | Format ablation decision rule: 90% compact parse rate threshold | Use compact format if compact parse rate ≥90% in 20-call ablation; use verbose otherwise | Below 90% compact success = measurable JSON extraction failures that will corrupt Phase 1 data. Verbose format trades token cost (higher) for parse reliability. Decision is irrevocable post-OSF registration — lock with evidence before Phase 1. D041 is a placeholder; actual per-model entries written after ablation runs in T02. | No — post-registration decisions are locked |
| D042 | M001/S04/planning | simulation | crash-resume double game_end is acceptable behavior | `resume_game()` appends round N+1..final to existing JSONL; the initial run's game_end event already in the file is left intact; resume produces a second game_end at end | A clean crash simulation requires running a partial game then resuming. The first game_end (from the truncated run) is a false termination; the second is the real one. For analysis, filter on the last game_end per game_id. The round_end events are the scientifically critical stream — no duplicates there is the correctness invariant. | Yes — could add a "resumed" flag to the second game_end to distinguish them |
| D043 | M001/S04/T02 | prompts | Format decision: llama (groq/llama-3.3-70b-versatile) | compact | compact 20/20 (100%), verbose 20/20 (100%); compact rate ≥90% threshold met; both formats identical for Llama — compact chosen for token efficiency | No — post-registration lock |
| D044 | M001/S04/T02 | prompts | Format decision: deepseek (openrouter/deepseek/deepseek-chat) | compact | compact 20/20 (100%), verbose 20/20 (100%); compact rate ≥90% threshold met; DeepSeek outputs minimal JSON regardless of prompt verbosity | No — post-registration lock |
| D045 | M001/S04/T02 | prompts | Format decision: gemini (gemini/gemini-2.5-flash) | compact | compact 19/20 (95%), verbose 20/20 (100%); compact rate ≥90% threshold met; one failure was prose reasoning ("I have enough for a tower…") — Gemini occasionally ignores JSON instruction on compact prompts but 95% is above threshold; borderline but acceptable | No — post-registration lock |
| D046 | M001/S04/T02 | prompts | Format decision: mistral (mistral/mistral-small-2506) | compact | compact 20/20 (100%), verbose 20/20 (100%); compact rate ≥90% threshold met; Mistral enforces JSON mode via response_format parameter making both formats equivalent; compact chosen | No — post-registration lock |
| D047 | M001/S04/T05 | prompts | Gemini response_format=json_object re-enabled (D021 superseded) | Add response_format={"type":"json_object"} to Gemini PROVIDER_KWARGS | D021 prohibited json_object mode because thinking tokens consumed the token budget causing content=None. With thinking={"type":"disabled","budget_tokens":0} (added in T02 ablation setup), no thinking tokens are consumed and json_object mode works reliably — verified 5/5 non-empty responses and 2 full clean 25-round games (aac41360 cost=$0.020, b83a4283 cost=$0.049, 0 parse failures). Without json_object, Gemini 2.5 Flash ~15-20% of rounds returned prose reasoning instead of JSON, causing forced hoard fallbacks and suppressing valid trade/build actions. | Yes — revert to prose+strategy3 parsing if future Gemini versions break json_object |
| D048 | M001/S05/design | architecture | Engine v2 full rebuild — agent-initiated trade via broadcast+match | Full rebuild of simulation engine replacing S04/T03-FIX patch sprint | Patch sprint (CF1–CF8 fixes) would have left reactive trade architecture and no degradation mechanics intact. First-principles redesign: (1) broadcast phase — agents declare wants publicly before acting; (2) targeted proposals based on market signals; (3) spoilage/destitution degradation; (4) hoard as active production. Design doc: docs/plans/2026-03-15-simulation-engine-v2-design.md | No — supersedes T03-FIX approach |
| D049 | M001/S05/design | simulation | Resource degradation mechanics: spoilage + destitution | Perishables (grain, fiber) decay above threshold=3; all-zero non-grain inventory triggers −1VP destitution penalty | Degradation creates urgency to trade surplus before it spoils (Catan model: resource scarcity drives cooperation). Destitution penalty prevents infinite hoarding. Together these break the 0% acceptance equilibrium by making inaction costly. | Yes — thresholds tunable in GameConfig |
| D050 | M001/S05/design | simulation | GameConfig v2 new fields | base_production=1, hoard_bonus=2, win_vp=10, perishable_resources=[grain,fiber], perishable_threshold=3, destitution_penalty_vp=-1, broadcast_phase=True, stream_events=True | base_production makes hoard a meaningful active choice (+2 vs passive +1). win_vp=10 lowered from 12 (3 buildings=9VP is now achievable). broadcast_phase enables agent-initiated trade market. | Yes — calibrate in S05/T04 validation |
| D051 | M001/S05/design | prompts | RULES_BLOCK shared constant in rules.py | Single static text block imported by all prompt modules | Prior design: each prompt had its own partial rules. Agents didn't see win condition, specialty mechanic, or hoard reward in act prompt. Agents didn't see round count in respond prompt. A shared RULES_BLOCK ensures all prompts use identical, complete, cache-friendly rules text. One update point for all agent knowledge. | No — correctness requirement |
| D052 | M001/S05/design | simulation | Broadcast phase before act phase each round | All agents emit {want, offer_hint} publicly; broadcasts shared in game_state before act() called | In reactive architecture, proposer had no signal that target wanted anything → cold-call proposals → 0% acceptance. Broadcast phase creates a public market board: agents see who wants what before proposing → targeted proposals with known demand → higher acceptance. Also produces new behavioral signal: strategic vs honest broadcasting. | Yes — could disable with broadcast_phase=False in GameConfig |
| D053 | M001/S05/design | prompts | Reflection output is structured JSON, stored as dict | reflect() returns and stores {survival_plan, next_building, best_trade_target, trade_strategy, relationships} dict | Prior: raw prose reflections (~200 chars) injected as pipe-separated text into act() user message — noisy, token-heavy, unparseable. Structured JSON: parseable for analysis, compressed to key fields for memory injection, avoids hallucination (fields constrain output). Memory injected as structured bullet points not raw prose. | No — correctness requirement for H4 analysis |
| D054 | M001/S05/design | observability | RoundMetrics computed per round | {gini, trade_acceptance_rate, avg_vp, vp_std, resource_diversity_index, starvation_count, spoilage_count} computed and logged as round_metrics event after every round | Prior: observability required post-hoc reconstruction from events. RoundMetrics enables real-time quality monitoring: if gini spikes early, game is degenerate; if trade_acceptance stays 0, prompt repair needed immediately. Critical for Phase 0 go/no-go signal without waiting for batch completion. | No — correctness requirement |
| D055 | M001/S05/design | observability | Optional rich console dashboard | dashboard=True in GameConfig enables rich Live() panel with VP bars, resource heatmap, trade stats | Research requires watching simulation quality in real time during Phase 0 calibration. Rich dashboard provides at-a-glance: who's winning, who's trading, who's starving. dashboard=False (default) keeps existing stdout line-per-round behavior — no change to non-dashboard runs. | Yes — disable if performance overhead exceeds 5% |
| D056 | M001/S07/T01 | research | Analysis stubs H1–H5 replaced to match CFIM/RNE design | New stubs: h1_self_play_premium, h2_mixed_effects, h3_identity_disclosure, h4_adaptation_lag, h5_cfim_to_multiagent. Old Harbour/Trade-Island stubs (h1_kruskal_wallis, h2_logistic_mixed_effects, h3_commons_exploitation, h4_architecture_vs_persona) retained in repo history but superseded. | The prior stubs were written for a different game design (Trade Island / Harbour primary study). The current design (CFIM/RNE as primary, Harbour as Study 2 ecological validity check) requires entirely different H1–H5. The new stubs match SIMULATION_DESIGN.md §5 exactly. | No — locked by pre-registration |
| D057 | M001/S07/T01 | research | H3 uses absolute value of disclosure effect (not signed) | `M6 = |M1_disclosed − M1_blind|` — two-sided. H3 tests magnitude > 0 and cross > same, NOT that disclosed > blind. | Long & Teplica (2025) showed disclosure can increase OR decrease cooperation depending on framing condition. Pre-registering a one-sided signed test would be wrong. The absolute value captures the presence of an effect without assuming its direction. | No — locked by pre-registration |
